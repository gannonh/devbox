{
  "name": "@gannonh/devbox",
  "version": "0.1.2",
  "description": "One-command isolated worktree dev containers with headed display, Pi agent, Chromium/OAuth, and GitHub-token forwarding.",
  "type": "module",
  "bin": {
    "devbox": "dist/cli.js"
  },
  "files": [
    "dist",
    "templates",
    "skills"
  ],
  "engines": {
    "node": ">=20.18.1"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "prepack": "npm run build",
    "worktree:setup": "bash scripts/worktree-setup.sh",
    "test": "vitest run",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "validate:release": "npm run build --silent && node dist/providers/vercel/release-validation.js"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@vercel/sandbox": "^3.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/node": "^26.0.1",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^2.0.0"
  }
}
